# @sapporta/grid

Grid runtime, React grid components, column presets, and lookup cache
primitives for Sapporta.

This package depends on `@sapporta/ui` for generic primitives. It does not
depend on Sapporta frontend routes, pages, or server/table/report contracts.
