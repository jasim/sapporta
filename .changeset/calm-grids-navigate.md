---
"@sapporta/frontend": minor
---

Let table pages control what happens when keyboard navigation reaches the edge
of the loaded rows. `TableGridView` keeps automatic page navigation by default,
while lower-level TGrid sessions can provide their own boundary handler.
